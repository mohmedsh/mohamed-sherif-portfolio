window.PORTFOLIO_CONFIG = {
  // Demo mode works immediately in this browser.
  // Production mode starts after adding a real Supabase URL and anon key.
  SUPABASE_URL: "YOUR_SUPABASE_URL",
  SUPABASE_ANON_KEY: "YOUR_SUPABASE_ANON_KEY",

  // The visible login name requested by the owner.
  AUTH_USERNAME: "mohamed.sherif",

  // Supabase itself authenticates by email. In production, replace this with
  // the email used when creating the owner in Supabase Authentication.
  AUTH_EMAIL: "YOUR_SUPABASE_OWNER_EMAIL",

  // Browser-only demo credentials. Do not use this password for private live data.
  DEMO_USERNAME: "mohamed.sherif",
  DEMO_PASSWORD: "123"
};
